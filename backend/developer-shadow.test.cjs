const {test}=require('node:test'),assert=require('node:assert/strict'),{compare}=require('./developer-shadow.cjs');
test('empty history never becomes a passing migration',()=>assert.equal(compare({},[]).status,'awaiting_history'));
test('missing exposure cannot be inferred as flat',()=>{const c={phase:'2-phase',currency:'USD',accountSize:10000,startDate:'2026-09-01T00:00:00Z'};assert.equal(compare(c,[{pnl:800,closeTime:'2026-09-01T16:00:00Z'}]).status,'blocked_event_coverage');});
test('complete synthetic input is only a candidate, never authorizes migration',()=>{const c={phase:'2-phase',currency:'USD',accountSize:10000,startDate:'2026-09-01T00:00:00Z',progressionMode:'automatic',ruleStatus:'active',lastRuleEvaluationSource:'broker_trade_trigger',ruleMetrics:{tradeCount:1},lastRuleEvaluationAt:'2026-09-01T17:00:00Z'};const r=compare(c,[{sequence:1,event_type:'closed_trade',timestamp:'2026-09-01T16:00:00Z',realized_pnl:'100',unrealized_pnl_after:'0',open_position_count:0,source_event_id:'one'}]);assert.equal(r.status,'candidate_match');assert.equal(r.eligibleForMigration,false);});
